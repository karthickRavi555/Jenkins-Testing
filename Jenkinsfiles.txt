node{
   stage('SCM Build'){
     svn 'http://192.168.18.45/svn/SilveradoStages/trunk/Jenkins/AccountManagement/'
   }
   stage('Compile-Package'){
      // Get maven home path
      def mvnHome =  tool name: 'maven-3', type: 'maven'   
      sh "${mvnHome}/bin/mvn package"
	  
	
   }
   
   